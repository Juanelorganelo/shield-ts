import { expect, test, describe } from "bun:test";
// import { type Brand, transparent, refined } from "../src";

describe('transparent', () => {
  test('creates a function with the given type', () => {
    expect.unreachable(`Not implemented`);
  });
});